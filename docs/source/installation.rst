Installation
============

BAccMod is currently available through pip. It can be installed with this command:

.. code-block:: bash

    pip install baccmod

Current dependencies are:

* gammapy >= 1.1
* numpy
* scipy
* astropy
* iminuit >= 2.0
* regions >= 0.7
